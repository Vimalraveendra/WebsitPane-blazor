@using Microsoft.JSInterop
@inject IJSRuntime JSRuntime

<nav class="navbar navbar-expand-sm bg-dark navbar-dark fixed">
    <div class="nav-top-list">
        <div>
            <a href="">Home</a>
            <a href="counter">About</a>
            <a href="FetchData">About</a>
        </div>
        <button @onclick="HandleNotes">Add Notes</button>
    </div>

</nav>
<NotesContainer myCustomEvent="@HandleNotes"></NotesContainer>
    @*<div class="notes">
        <div class="notes-group">
            <p>Notes container</p>
            <button @onclick="HandleNotes">close</button>
        </div>
        <h4>Hello</h4>
    </div>*@


@code {

 

    public async void HandleNotes()
    {
        @*showNote = !showNote;*@
        await JSRuntime.InvokeVoidAsync("HandleEvents");
    }

}
